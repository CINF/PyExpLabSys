<sockets>
    <!-- Gas alarm 307 -->
    <socket>rasppi34:8000</socket>

    <!-- Gas alarm 312 -->
    <socket>rasppi15:8000</socket>

    <!-- Micro-reactor NG temperature -->
    <socket>rasppi12:8000</socket>

    <!-- Micro-reactor NG mass spec -->
    <socket>cinf-microreactorNG-ms:8000</socket>

    <!-- Self -->
    <socket>localhost:8000</socket>

    <!-- old micro reactor, Ion Gauge Pressure -->
    <!-- mr_iongauge_pressur -->
    <socket>rasppi07:8000</socket>

    <!-- old micro reactor, Temperature of sample measured with TC -->
    <!-- mr_sample_tc_temperature -->
    <socket>rasppi12:8000</socket>

    <!-- old micro-reactor mass spec -->
    <socket>cinf-microreactor-ms:8000</socket>

    <!-- Old micro-reactor pressure in buffer volume (mr_buffer_pressure) AND micro-reactor NG pressure in buffer volume microreactorng_pressure_buffer -->
    <!-- mr_buffer_pressure, microreactorng_pressure_buffer -->
    <socket>rasppi36:8000</socket>

    <!-- Flow and pressure controllers, old microreactor, Palle and PS -->
    <!-- M13201551A, M8203814C, M11200362F, M8203814A, M8203814B, M11200362B
         M11200362H -->
    <socket>rasppi24:8000</socket>

    <!-- Flow and pressure controllers, old microreactor and sniffer setup -->
    <!-- 21984839, 21984838 -->
    <socket>rasppi37:8000</socket>

    <!-- Sniffer analog pressure controller -->
    <socket>rasppi06:8000</socket>

    <!-- sniffer_qms_ion_gauge - Sniffer, Ion Gauge pressure at mass spec
	 sniffer_qms_roughing - Sniffer, roughing for qms turbo
	 sniffer_buffer_roughing  - Sniffer, roughing for buffer turbo
	 sniffer_buffer_pressure  - Sniffer, buffer pirani pressure -->
    <socket>rasppi107:8000</socket>

    <!-- XRD flow controllers -->
    <socket>rasppi16:8000</socket>

    <!-- Parrallel screening pressure read out -->
    <socket>rasppi28:8000</socket>

    <!-- Microreactor Analogue flow controller -->
    <socket>rasppi26:8000</socket>

    <!-- PS Turbo Pump -->
    <socket>rasppi28:8000</socket>

    <!-- b309PVD temperatures -->
    <socket>rasppi112:8000</socket>

    <!-- b309PVD pressures -->
    <socket>rasppi51:8000</socket>

    <!-- b309PVD QCMs -->
    <socket>rasppi106:8000</socket>

    <!-- Liveplot as mass-spec data for leak searching -->
    <socket>cinf-volvo:8000</socket>

</sockets>
